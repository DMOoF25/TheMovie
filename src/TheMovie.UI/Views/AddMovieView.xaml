<Page x:Class="TheMovie.UI.Views.AddMovieView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:TheMovie.UI.Converters"
      mc:Ignorable="d"
      Title="Tilføj film">

    <Page.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <local:NullToVisibilityConverter x:Key="NullToVisibilityConverter"/>
    </Page.Resources>

    <StackPanel Orientation="Vertical">
        <TextBlock Text="Tilføj film" FontSize="24" FontWeight="Bold" Margin="0 0 0 10"/>
        <StackPanel Orientation="Horizontal">
            <StackPanel Orientation="Vertical">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Titel:" VerticalAlignment="Center" />
                    <TextBox
                        Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}"
                        MinWidth="250"/>

                    <TextBlock Text="Varighed (min):" VerticalAlignment="Center" />
                    <TextBox 
                        Text="{Binding DurationText, UpdateSourceTrigger=PropertyChanged}"
                        PreviewTextInput="Duration_PreviewTextInput"
                        MinWidth="60"/>
                </StackPanel>
                <TextBlock Text="Genrer:" VerticalAlignment="Top" />
                <ScrollViewer Height="120" VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding GenreOptions}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <CheckBox Content="{Binding Name}" IsChecked="{Binding IsSelected, Mode=TwoWay}" />
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>

                <TextBlock
                   Text="{Binding Error}"
                   Foreground="Red"
                   TextWrapping="Wrap"
                   Visibility="{Binding Error, Converter={StaticResource NullToVisibilityConverter}}"/>
            </StackPanel>
        </StackPanel>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Gem" Width="80" Command="{Binding SaveCommand}" IsDefault="True"/>
                <Button Content="Nulstil" Width="80" Command="{Binding ResetCommand}"/>
                <Button Content="Annuller" Width="80" Command="{Binding CancelCommand}"/>
            </StackPanel>

    </StackPanel>
</Page>